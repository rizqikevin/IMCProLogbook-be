# Legacy filename: new installations use .env.example -> .env on both platforms.
COMPOSE_PROJECT_NAME=machine-logbook
POSTGRES_PASSWORD=
APP_ENV=production
STORAGE_DRIVER=local
WEB_PORT=3000
CORS_ALLOWED_ORIGINS=
TUNNEL_TOKEN=

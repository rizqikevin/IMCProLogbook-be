# Compatibility entry point for existing home deployments; no separate stack.
# Keep the previous project name so its database volume is reused.
name: machine-logbook
include:
  - path: docker-compose.yml

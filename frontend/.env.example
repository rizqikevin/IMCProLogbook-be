# Vite development proxy. Never place secrets in VITE_* variables.
API_PROXY_TARGET=http://127.0.0.1:8080

# Leave empty for the development proxy and same-origin production deployment.
VITE_API_BASE_URL=
